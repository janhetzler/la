name: Frontmatter Lint

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: OKF-Frontmatter pruefen
        run: python3 scripts/ci/lint_frontmatter.py
